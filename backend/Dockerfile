# syntax=docker/dockerfile:1
FROM golang:1.22 AS build
WORKDIR /app

COPY go.mod go.sum ./
RUN go mod download

COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o /out/aegiscloud-server ./cmd/server

FROM gcr.io/distroless/static-debian12
COPY --from=build /out/aegiscloud-server /aegiscloud-server
EXPOSE 8080
ENTRYPOINT ["/aegiscloud-server"]
